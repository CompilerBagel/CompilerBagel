.global main
.data
    getch:
    before_main:
    getfarray:
    putint:
    putfarray:
    end_time:
    getarray:
    after_main:
    getint:
    putarray:
    start_time:
    putfloat:
    putch:
    getfloat:
.text
main:
mainEntry1:
    addi sp, sp, -1632
    sd ra, 1624(sp)
    sd s0, 1616(sp)
    addi s0, sp, 1632
    addi a1, s0, -52
    addi a1, s0, -52
    li a1, 0
    sw a1, -52(s0)
    addi a1, s0, -52
    addi a1, s0, -56
    li a1, 0
    sw a1, -56(s0)
    addi a1, s0, -60
    addi a1, s0, -60
    li a1, 0
    sw a1, -60(s0)
    addi a1, s0, -60
    addi a1, s0, -64
    li a1, 0
    sw a1, -64(s0)
    addi a1, s0, -68
    addi a1, s0, -68
    li a1, 0
    sw a1, -68(s0)
    addi a1, s0, -68
    addi a1, s0, -72
    li a1, 0
    sw a1, -72(s0)
    addi a1, s0, -76
    addi a1, s0, -76
    li a1, 0
    sw a1, -76(s0)
    addi a1, s0, -76
    addi a1, s0, -80
    li a1, 0
    sw a1, -80(s0)
    addi a1, s0, -104
    addi a1, s0, -104
    li a1, 1
    sw a1, -104(s0)
    addi a1, s0, -104
    addi a1, s0, -108
    li a1, 2
    sw a1, -108(s0)
    addi a1, s0, -112
    addi a1, s0, -112
    li a1, 3
    sw a1, -112(s0)
    addi a1, s0, -112
    addi a1, s0, -116
    li a1, 4
    sw a1, -116(s0)
    addi a1, s0, -120
    addi a1, s0, -120
    li a1, 5
    sw a1, -120(s0)
    addi a1, s0, -120
    addi a1, s0, -124
    li a1, 6
    sw a1, -124(s0)
    addi a1, s0, -128
    addi a1, s0, -128
    li a1, 7
    sw a1, -128(s0)
    addi a1, s0, -128
    addi a1, s0, -132
    li a1, 8
    sw a1, -132(s0)
    addi a1, s0, -208
    addi a1, s0, -208
    li a1, 1
    sw a1, -208(s0)
    addi a1, s0, -208
    addi a1, s0, -212
    li a1, 2
    sw a1, -212(s0)
    addi a1, s0, -216
    addi a1, s0, -216
    li a1, 3
    sw a1, -216(s0)
    addi a1, s0, -216
    addi a1, s0, -220
    li a1, 4
    sw a1, -220(s0)
    addi a1, s0, -224
    addi a1, s0, -224
    li a1, 5
    sw a1, -224(s0)
    addi a1, s0, -224
    addi a1, s0, -228
    li a1, 6
    sw a1, -228(s0)
    addi a1, s0, -232
    addi a1, s0, -232
    li a1, 7
    sw a1, -232(s0)
    addi a1, s0, -232
    addi a1, s0, -236
    li a1, 8
    sw a1, -236(s0)
    addi a1, s0, -416
    addi a1, s0, -416
    li a1, 1
    sw a1, -416(s0)
    addi a1, s0, -416
    addi a1, s0, -420
    li a1, 2
    sw a1, -420(s0)
    addi a1, s0, -424
    addi a1, s0, -424
    li a1, 3
    sw a1, -424(s0)
    addi a1, s0, -424
    addi a1, s0, -428
    li a1, 0
    sw a1, -428(s0)
    addi a1, s0, -432
    addi a1, s0, -432
    li a1, 5
    sw a1, -432(s0)
    addi a1, s0, -432
    addi a1, s0, -436
    li a1, 0
    sw a1, -436(s0)
    addi a1, s0, -440
    addi a1, s0, -440
    li a1, 7
    sw a1, -440(s0)
    addi a1, s0, -440
    addi a1, s0, -444
    li a1, 8
    sw a1, -444(s0)
    addi a1, s0, -432
    addi a1, s0, -436
    lw a1, -436(s0)
    addi a2, s0, -224
    addi a2, s0, -228
    lw a2, -228(s0)
    addi a3, s0, -832
    addi a3, s0, -832
    sw a1, -832(s0)
    addi a1, s0, -832
    addi a1, s0, -836
    sw a2, -836(s0)
    addi a1, s0, -840
    addi a1, s0, -840
    li a1, 3
    sw a1, -840(s0)
    addi a1, s0, -840
    addi a1, s0, -844
    li a1, 4
    sw a1, -844(s0)
    addi a1, s0, -848
    addi a1, s0, -848
    li a1, 5
    sw a1, -848(s0)
    addi a1, s0, -848
    addi a1, s0, -852
    li a1, 6
    sw a1, -852(s0)
    addi a1, s0, -856
    addi a1, s0, -856
    li a1, 7
    sw a1, -856(s0)
    addi a1, s0, -856
    addi a1, s0, -860
    li a1, 8
    sw a1, -860(s0)
    addi a1, s0, -856
    addi a1, s0, -860
    lw a1, -860(s0)
    addi a2, s0, -832
    addi a2, s0, -832
    lw a2, -832(s0)
    addw a1, a1, a2
    addi a2, s0, -832
    addi a2, s0, -836
    lw a2, -836(s0)
    addw a1, a1, a2
    addi a2, s0, -68
    addi a2, s0, -68
    lw a2, -68(s0)
    addw a1, a1, a2
    addw a0, a1, 0
    ld ra, 1624(sp)
    ld s0, 1616(sp)
    addi sp, sp, 1632
    ret
    li a0, 0
    ret
